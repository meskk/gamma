# Copy to .env.local for local dev. .env.local is gitignored.
# Base URL of the core API (note the /v1 prefix). The CORS allowlist on the backend
# (GAMMA_CORS_ORIGIN) must include this frontend's origin.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/v1
